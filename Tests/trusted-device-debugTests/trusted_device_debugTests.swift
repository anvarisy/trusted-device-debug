import XCTest
@testable import trusted_device_debug

final class trusted_device_debugTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(trusted_device_debug().text, "Hello, World!")
    }
}
